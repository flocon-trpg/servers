import React from 'react';

export namespace MyStyle {
    export namespace Text {
        export const larger: React.CSSProperties = { fontSize: 16, fontWeight: 'bold' };
        export const large: React.CSSProperties = { fontSize: 22, fontWeight: 'bold' };
    }
}