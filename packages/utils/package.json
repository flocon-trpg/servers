{
    "version": "0.1.0-beta.1",
    "license": "MIT",
    "main": "dist/cjs/index.js",
    "module": "dist/esm/index.js",
    "typings": "dist/types/index.d.ts",
    "files": [
        "dist"
    ],
    "engines": {
        "node": ">=12"
    },
    "scripts": {
        "build": "yarn run build:self",
        "build-ci": "yarn run build:self",
        "build:self": "rimraf ./dist && yarn build:tsc",
        "build:tsc": "tsc -p tsconfig.build-cjs.json && tsc -p tsconfig.build-esm.json && tsc -p tsconfig.build-types.json",
        "test": "jest"
    },
    "name": "@flocon-trpg/utils",
    "author": "kizahasi",
    "description": "One of core libraries for Flocon",
    "repository": {
        "type": "git",
        "url": "https://github.com/flocon-trpg/servers"
    },
    "dependencies": {
        "@kizahasi/option": "^1.0.0"
    },
    "devDependencies": {
        "@types/jest": "27.0.3",
        "jest": "27.4.3",
        "rimraf": "3.0.2",
        "ts-jest": "27.1.0",
        "typescript": "4.4.4"
    }
}
