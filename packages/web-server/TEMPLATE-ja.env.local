# 使い方
# 1. このファイルをこのディレクトリ内にコピーします。
# 2. コピーしたファイルを `.env.local`（もしくは`.env`）という名前にリネームします。` は名前に含めてはなりません。
# 3. 説明に従ってファイル内の値を変更します。

# すべての値を.env.localや.envで記述する必要はありません。代わりに環境変数を使用することも可能です。
# このファイルは、Flocon Webサーバーにおいて使用可能な環境変数の一覧のドキュメントとしての役割も兼ねています。


# Firebase構成オブジェクトの設定
# Firebaseの管理ページにあるデータを参照して、*の部分を置き換えてください。
# measurementId は必要ありません。
# 通常は、APIサーバーの NEXT_PUBLIC_FIREBASE_CONFIG と全く同じ値になります。
NEXT_PUBLIC_FIREBASE_CONFIG='{"apiKey":"****************","authDomain":"***-*****.firebaseapp.com","databaseURL":"https://***-*****.firebaseio.com","projectId":"***-*****","storageBucket":"***-*****.appspot.com","messagingSenderId":"**********","appId":"****************"}'

# APIサーバーのHTTPのアドレスの設定
# 通常は https:// もしくは http:// で始まります。
# 入力例: NEXT_PUBLIC_API_HTTP=https://example.com/
NEXT_PUBLIC_API_HTTP=http://localhost:4000

# APIサーバーのWebSocketのアドレスの設定
# 通常は、NEXT_PUBLIC_API_HTTP の https:// もしくは http:// の部分をそれぞれ wss:// と ws:// に置き換えたものになります。
# 入力例: NEXT_PUBLIC_API_WS=wss://example.com
NEXT_PUBLIC_API_WS=ws://localhost:4000

# Firebase Authenticationで有効化したログイン方法の設定
# 有効化したログイン方法を全て入力します。複数ある場合は,で区切ります。
# 使用可能な値は、email、google、facebook、github、twitter、phoneです（後で他のログイン方法も追加する予定です）。
# 例えばメールパスワードとGoogleの2つのログイン方法を有効化した場合は、下のように email,google と入力します。
NEXT_PUBLIC_AUTH_PROVIDERS=email,google

# Firebase Storageアップローダーの有効化の設定
# Firebase Storageアップローダーを有効化する場合は true を、そうでない場合は false を入力します。
# true にする場合は、あわせて Firebase 管理ページから Firebase Storage サービスを有効化する必要があります。
NEXT_PUBLIC_FIREBASE_STORAGE_ENABLED=false

